[build]
  command = "npm ci && ng build --configuration production"
  publish = "dist/CE_Massage_Frontent/browser"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200